<section class="colab-wrap">
  <section class="colab-page-hero">
    <div class="colab-container">
      <div class="colab-header--left">
        <p class="colab-eyebrow">Account</p>
        <h1>Reset your password</h1>
        <div class="colab-lead" style="margin-top:18px;">Choose a new password for your account.</div>
      </div>
    </div>
  </section>

  <section class="colab-section colab-surface">
    <div class="colab-container">
      <div class="colab-page-card colab-account-card">
        {% form 'reset_customer_password' %}
          {% if form.errors %}
            <div class="colab-errors">{{ form.errors | default_errors }}</div>
          {% endif %}
          <div class="colab-form-stack">
            <input class="colab-input" type="password" name="customer[password]" placeholder="New password" autocomplete="new-password" required>
            <input class="colab-input" type="password" name="customer[password_confirmation]" placeholder="Confirm new password" autocomplete="new-password" required>
          </div>
          <div class="colab-btn-row">
            <button class="colab-btn colab-btn--primary" type="submit">Reset password</button>
          </div>
        {% endform %}
      </div>
    </div>
  </section>
</section>
{% schema %}
{
  "name": "CoLab Reset Password",
  "settings": []
}
{% endschema %}
